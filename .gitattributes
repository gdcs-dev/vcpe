# Keep main's openspec/ unchanged when merging from other branches.
# The ours driver always keeps the current branch's version of matched files.
# Requires one-time git config: git config merge.ours.driver true
openspec/** merge=ours
