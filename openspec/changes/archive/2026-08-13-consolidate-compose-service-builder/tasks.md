## 1. Baseline Characterization

- [x] 1.1 Confirm each of the 7 built-in types has a golden/characterization test that locks down its current Compose service-block shape (image, container_name, hostname, env_file, networks, ports) and, where applicable, its health-sidecar YAML; add any missing coverage before touching production code. Run `go test ./internal/types/...`. Confirmed existing coverage is sufficient: `port_parity_test.go`'s `TestRendererComposeSemantics`/`TestRenderersPreserveManifestPorts` already assert container_name/hostname/ports/mac+ipv4-pinning per type; `gateway_golden_test.go`'s `TestGatewayRendersHealthSidecarOnlyWhenHealthUpstreamDeclared` and `genericcontainer_golden_test.go`'s `TestGenericContainerRendersConfiguredHealthSidecar`/`TestGenericContainerWithoutHealthHasNoEndpoint` already assert the two sidecar shapes. No new characterization tests were needed.
- [x] 1.2 Record the exact current field-by-field shape of `gateway`'s proxy sidecar and `generic-container`'s probe sidecar (image, container_name/service naming, entrypoint/command, networks, ports, restart, depends_on) as the byte-level target the shared builders must reproduce. Recorded by direct source inspection; also discovered a real, pre-existing three-way network-attachment split (not two): `bng`/`webpa` pin ipv4 conditionally on network-managed status, `oktopus`/`event-sink` pin ipv4 **unconditionally** (ignoring managed status), `gateway`/`xb10` never pin ipv4. Resolution: keep only two named shared helpers (`DefaultAttachment`, `MACOnlyAttachment`) per the design's cap; `oktopus`/`event-sink` supply their own small local closure to preserve their existing unconditional-pin behavior exactly, rather than adding a third shared helper or silently changing their behavior. Also found during 3.2: `DefaultAttachment` must compute "managed" via `deployment.Network(role).IPAMDriver` lookup (matching every type's pre-existing inline logic exactly), not via `plan.Interface.ManagedNetwork` — the latter is only populated by the real planner and is left zero-valued by hand-built `plan.Instance` fixtures in existing tests, which caused a `TestRendererComposeSemantics/webpa` regression that was caught and fixed before proceeding.

## 2. Shared Compose and Health-Sidecar Builders

- [x] 2.1 Add `DefaultAttachment` and `MACOnlyAttachment` network-attachment helpers to `internal/render/servicetemplate`, each with focused tests for managed/unmanaged networks; run `go test ./internal/render/servicetemplate`. Implemented as `func(iface plan.Interface) map[string]any` using the planner-resolved `iface.ManagedNetwork` field directly (equivalent to, and simpler than, the `IPAMDriver`-lookup each type previously computed inline).
- [x] 2.2 Add `BuildComposeService` producing the standard image/container_name/hostname/env_file/networks fields, with tests for naming, env_file path, and network population via both attachment helpers.
- [x] 2.3 Add `AttachProxySidecar` and `AttachProbeSidecar` reproducing the exact recorded shapes from 1.2, with tests for the sidecar YAML, service naming, and network wiring in isolation from any real service type.
- [x] 2.4 Escape hatch implemented simpler than originally speced: no `ComposeExtras` hook was added to `Hooks[C]`/the shared lifecycle. Each type's own `RenderInstance` already owns full construction of its `compose.yaml` fragment, so it calls `BuildComposeService`/`AttachProxySidecar`/`AttachProbeSidecar` and then sets any remaining fields (`privileged`, `cap_add`, `volumes`, `network_mode`) directly on the returned `map[string]any` as ordinary Go code — no new hook plumbing through the core renderer was needed or added. This still satisfies the design's cap (no per-field flags in the shared framework) with less new surface than planned.
- [x] 2.5 Run `go test ./internal/render/servicetemplate` and confirm all new shared-builder tests pass before any type migrates. All pass (`compose_test.go`).

## 3. Low-Risk Type Migrations

- [x] 3.1 Migrate `event-sink` to `BuildComposeService` + a local always-pin attachment closure (preserving its existing unconditional ipv4 pinning, see 1.2 note), deleting its inline Compose construction; run `go test ./internal/types/eventsink ./internal/render/... ./internal/types`.
- [x] 3.2 Migrate `webpa` to `BuildComposeService` + `DefaultAttachment`, setting `privileged`/`cap_add` directly on the returned map; run `go test ./internal/types/webpa ./internal/render/... ./internal/types`. Caught and fixed a real regression here: `DefaultAttachment` must derive "managed" from `deployment.Network(role).IPAMDriver`, not the planner-only `plan.Interface.ManagedNetwork` field (see 1.2 note).
- [x] 3.3 Migrate `oktopus` to `BuildComposeService` + a local always-pin attachment closure, deleting `generateCompose`; run `go test ./internal/types/oktopus ./internal/render/... ./internal/types`.
- [x] 3.4 Compare each migrated type's artifact inventory and parsed Compose output against its pre-migration golden/characterization tests; resolve any mismatch before proceeding. All of `TestRendererArtifactInventory`/`TestRenderersPreserveManifestPorts`/`TestRendererComposeSemantics` pass unchanged for event-sink, webpa, and oktopus.

## 4. MAC-Only and Proxy-Sidecar Migrations

- [x] 4.1 Migrate `xb10` to `BuildComposeService` + `MACOnlyAttachment`, preserving its addressing exemption; run `go test ./internal/types/xb10 ./internal/render/... ./internal/types`.
- [x] 4.2 Migrate `gateway` to `BuildComposeService` + `MACOnlyAttachment` + `AttachProxySidecar` (triggered by its existing `hasHealthUpstream` check), setting `privileged`/`cap_add`/`volumes` directly on the returned map, deleting `renderGatewayCompose`; run `go test ./internal/types/gateway ./internal/render/... ./internal/types`.
- [x] 4.3 Verify gateway's health-sidecar-present and health-sidecar-absent golden tests both still pass byte-for-byte-equivalent Compose output. `TestGatewayRendersHealthSidecarOnlyWhenHealthUpstreamDeclared` passes unchanged.

## 5. Interpolated and Auxiliary-Heavy Migrations

- [x] 5.1 Migrate `generic-container`'s probe sidecar to `AttachProbeSidecar` (triggered by its existing `cfg.Health != nil` check); its main workload block stays on its own construction (interpolated `${...}` tokens and replica-conditional MAC pinning are not compatible with `BuildComposeService`'s literal-value contract — see design.md Non-Goals). Keep `Interpolated` mode, `${...}` references, and its `entrypoint.sh` artifact unchanged; run `go test ./internal/types/genericcontainer ./internal/render/... ./internal/types`.
- [x] 5.2 Migrate `bng` to `BuildComposeService`, setting `privileged`/`cap_add`/`volumes` directly on the returned map, preserving all DHCP/RADVD/DNS/sysctl/firewall/network-derived auxiliary artifacts unchanged; run `go test ./internal/types/bng ./internal/render/... ./internal/types`. Bonus cleanup: bng's local `ipamNone` map was fully redundant with `BuildComposeService`'s internal managed-status computation (both derive from the same `IPAMDriver == "none"` check on `input.Deployment.Networks`), so it was deleted rather than threaded through as a second attachment override.
- [x] 5.3 Compare generic-container and bng exact environment/entrypoint/auxiliary-artifact expectations plus parsed Compose output against their characterization tests. All existing bng and generic-container package tests pass unchanged.

## 6. Cleanup and Final Verification

- [x] 6.1 Delete all now-dead per-type `render*Compose`/`generateCompose` functions and any now-unused helpers. Revised on inspection: these functions are not dead, they remain each type's thin wrapper that calls `BuildComposeService`/`AttachProxySidecar`/`AttachProbeSidecar` and sets its own extra fields — exactly the intended escape-hatch pattern (see 2.4). What was deleted: the duplicated per-interface network-attachment/external-network construction loops and the duplicated core-field map literals in all 7 types, plus bng's now-redundant local `ipamNone` computation. Net: 7 type files -99 lines; 1 new shared file (`compose.go`, 103 lines) is the single owner of that logic going forward.
- [x] 6.2 Run `gofmt` on all changed files and confirm clean diagnostics. Clean (the one file `gofmt -l` flagged, `internal/hostnet/adapter_test.go`, is untouched by this change and pre-existing).
- [x] 6.3 Run `go test ./internal/render/... ./internal/typeregistry ./internal/types/...` and confirm no regressions. All pass.
- [x] 6.4 Run `go test ./...` and `go build ./...` from `controlplane` and document only pre-existing or environment-gated failures. Build clean; only `TestRunRelease_CoherenceFailure` fails (pre-existing, environment-gated on the `main` branch check — unrelated to this change).
- [x] 6.5 Review the final diff to confirm the escape-hatch surface stayed capped at `ComposeExtras` + the two attachment helpers + the two sidecar builders — no additional flags or hooks were added along the way. Confirmed: `servicetemplate.go` (the core `Hooks[C]`/renderer lifecycle) has zero diff. The only new shared surface is `NetworkAttachment` + `DefaultAttachment`/`MACOnlyAttachment` + `BuildComposeService` + `AttachProxySidecar`/`AttachProbeSidecar` in the new `compose.go`. No `ComposeExtras` hook was added to the framework (see 2.4); the escape hatch is ordinary Go code in each type's own function, which is simpler than originally speced and still fully capped.
- [x] 6.6 Sync the `controlplane-code-reuse` capability spec delta (this change's `specs/`) into `openspec/specs/` and confirm `openspec validate consolidate-compose-service-builder`. Applied: replaced the "Shared renderer lifecycle" and "Consolidation boundaries" requirements and added the two new requirements in `openspec/specs/controlplane-code-reuse/spec.md`. Both `openspec validate consolidate-compose-service-builder --strict` and `openspec validate controlplane-code-reuse --strict` pass.
